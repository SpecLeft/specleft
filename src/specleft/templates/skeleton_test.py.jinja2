"""
Auto-generated skeleton tests from features.json.
Regenerate using: specleft test skeleton

Generated by SpecLeft - https://github.com/yourusername/spec-left
"""

import pytest

from specleft import specleft

{% for feature in features %}
# =============================================================================
# Feature: {{ feature.name }}
# ID: {{ feature.id }}
{% if feature.description %}
# {{ feature.description }}
{% endif %}
# =============================================================================

{% for scenario in feature.scenarios %}
@specleft(feature_id="{{ feature.id }}", scenario_id="{{ scenario.id }}")
{% if scenario.test_data %}
@pytest.mark.parametrize(
    "{{ scenario.test_data[0].params.keys() | list | join(', ') }}",
    [
{% for data_row in scenario.test_data %}
        ({{ data_row.params.values() | list | map('repr') | join(', ') }}),  # {{ data_row.description or '' }}
{% endfor %}
    ],
)
{% endif %}
def test_{{ scenario.id | snake_case }}({% if scenario.test_data %}{{ scenario.test_data[0].params.keys() | list | join(', ') }}{% endif %}):
    """{{ scenario.name }}

{% if scenario.description %}
    {{ scenario.description }}

{% endif %}
    Priority: {{ scenario.priority.value }}
{% if scenario.tags %}
    Tags: {{ scenario.tags | join(', ') }}
{% endif %}
    """
{% for step in scenario.steps %}
    with specleft.step("{{ step.type.value | capitalize }} {{ step.description }}"):
        pass  # TODO: Implement step logic

{% endfor %}

{% endfor %}
{% endfor %}
