<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SpecLeft Test Report</title>
    <style>
        :root {
            --color-pass: #22c55e;
            --color-fail: #ef4444;
            --color-skip: #eab308;
            --color-bg: #f8fafc;
            --color-card: #ffffff;
            --color-border: #e2e8f0;
            --color-text: #1e293b;
            --color-text-muted: #64748b;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: var(--color-bg);
            color: var(--color-text);
            line-height: 1.6;
            padding: 2rem;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            margin-bottom: 2rem;
        }

        h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .run-info {
            color: var(--color-text-muted);
            font-size: 0.875rem;
        }

        /* Summary Cards */
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .summary-card {
            background: var(--color-card);
            border: 1px solid var(--color-border);
            border-radius: 0.5rem;
            padding: 1.5rem;
            text-align: center;
        }

        .summary-card.passed { border-top: 4px solid var(--color-pass); }
        .summary-card.failed { border-top: 4px solid var(--color-fail); }
        .summary-card.skipped { border-top: 4px solid var(--color-skip); }

        .summary-value {
            font-size: 2rem;
            font-weight: 700;
        }

        .summary-value.passed { color: var(--color-pass); }
        .summary-value.failed { color: var(--color-fail); }
        .summary-value.skipped { color: var(--color-skip); }

        .summary-label {
            color: var(--color-text-muted);
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Progress Bar */
        .progress-bar {
            background: var(--color-border);
            border-radius: 9999px;
            height: 0.75rem;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .progress-fill {
            height: 100%;
            display: flex;
        }

        .progress-passed { background: var(--color-pass); }
        .progress-failed { background: var(--color-fail); }
        .progress-skipped { background: var(--color-skip); }

        /* Feature Section */
        .feature {
            background: var(--color-card);
            border: 1px solid var(--color-border);
            border-radius: 0.5rem;
            margin-bottom: 1.5rem;
            overflow: hidden;
        }

        .feature-header {
            padding: 1rem 1.5rem;
            background: #f1f5f9;
            border-bottom: 1px solid var(--color-border);
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .feature-header:hover {
            background: #e2e8f0;
        }

        .feature-title {
            font-size: 1.125rem;
            font-weight: 600;
        }

        .feature-id {
            color: var(--color-text-muted);
            font-size: 0.875rem;
            font-weight: normal;
        }

        .feature-stats {
            display: flex;
            gap: 1rem;
            font-size: 0.875rem;
        }

        .stat-passed { color: var(--color-pass); }
        .stat-failed { color: var(--color-fail); }
        .stat-skipped { color: var(--color-skip); }

        .feature-content {
            padding: 1rem 1.5rem;
        }

        /* Scenario */
        .scenario {
            border: 1px solid var(--color-border);
            border-radius: 0.375rem;
            margin-bottom: 1rem;
        }

        .scenario:last-child {
            margin-bottom: 0;
        }

        .scenario-header {
            padding: 0.75rem 1rem;
            background: #fafafa;
            border-bottom: 1px solid var(--color-border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .scenario-name {
            font-weight: 500;
        }

        .scenario-id {
            color: var(--color-text-muted);
            font-size: 0.75rem;
        }

        .badge {
            padding: 0.25rem 0.5rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .badge-passed { background: #dcfce7; color: #166534; }
        .badge-failed { background: #fee2e2; color: #991b1b; }
        .badge-skipped { background: #fef9c3; color: #854d0e; }

        /* Tags */
        .tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.25rem;
            margin-top: 0.25rem;
        }

        .tag {
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
            font-size: 0.625rem;
            font-weight: 500;
            background: #e0e7ff;
            color: #3730a3;
            text-transform: lowercase;
        }

        /* Executions */
        .executions {
            padding: 0.75rem 1rem;
        }

        .execution {
            padding: 0.5rem 0;
            border-bottom: 1px solid var(--color-border);
        }

        .execution:last-child {
            border-bottom: none;
        }

        .execution-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .execution-name {
            font-family: monospace;
            font-size: 0.875rem;
        }

        .execution-duration {
            color: var(--color-text-muted);
            font-size: 0.75rem;
        }

        .params {
            background: #f1f5f9;
            padding: 0.5rem;
            border-radius: 0.25rem;
            font-family: monospace;
            font-size: 0.75rem;
            margin-bottom: 0.5rem;
        }

        /* Steps */
        .steps {
            margin-top: 0.5rem;
        }

        .step {
            display: flex;
            align-items: flex-start;
            gap: 0.5rem;
            padding: 0.25rem 0;
            font-size: 0.875rem;
        }

        .step-icon {
            width: 1rem;
            height: 1rem;
            flex-shrink: 0;
            margin-top: 0.125rem;
        }

        .step-icon.passed { color: var(--color-pass); }
        .step-icon.failed { color: var(--color-fail); }

        .step-description {
            flex: 1;
        }

        .step-duration {
            color: var(--color-text-muted);
            font-size: 0.75rem;
        }

        /* Error Display */
        .error-message {
            background: #fef2f2;
            border: 1px solid #fecaca;
            border-radius: 0.25rem;
            padding: 0.75rem;
            margin-top: 0.5rem;
            font-family: monospace;
            font-size: 0.75rem;
            color: #991b1b;
            white-space: pre-wrap;
            overflow-x: auto;
        }

        /* Skip Reason */
        .skip-reason {
            color: var(--color-text-muted);
            font-size: 0.75rem;
            font-style: italic;
            margin-top: 0.25rem;
        }

        /* Footer */
        footer {
            margin-top: 2rem;
            padding-top: 1rem;
            border-top: 1px solid var(--color-border);
            text-align: center;
            color: var(--color-text-muted);
            font-size: 0.875rem;
        }

        /* Responsive */
        @media (max-width: 640px) {
            body {
                padding: 1rem;
            }

            .summary-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .feature-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>SpecLeft Test Report</h1>
            <p class="run-info">Run ID: {{ results.run_id }}</p>
        </header>

        <!-- Summary Cards -->
        <div class="summary-grid">
            <div class="summary-card">
                <div class="summary-value">{{ results.summary.total_executions }}</div>
                <div class="summary-label">Total Tests</div>
            </div>
            <div class="summary-card passed">
                <div class="summary-value passed">{{ results.summary.passed }}</div>
                <div class="summary-label">Passed</div>
            </div>
            <div class="summary-card failed">
                <div class="summary-value failed">{{ results.summary.failed }}</div>
                <div class="summary-label">Failed</div>
            </div>
            <div class="summary-card skipped">
                <div class="summary-value skipped">{{ results.summary.skipped }}</div>
                <div class="summary-label">Skipped</div>
            </div>
            <div class="summary-card">
                <div class="summary-value">{{ "%.2f"|format(results.summary.duration) }}s</div>
                <div class="summary-label">Duration</div>
            </div>
        </div>

        <!-- Progress Bar -->
        {% set total = results.summary.total_executions %}
        {% if total > 0 %}
        <div class="progress-bar">
            <div class="progress-fill">
                <div class="progress-passed" style="width: {{ (results.summary.passed / total * 100)|round(1) }}%"></div>
                <div class="progress-failed" style="width: {{ (results.summary.failed / total * 100)|round(1) }}%"></div>
                <div class="progress-skipped" style="width: {{ (results.summary.skipped / total * 100)|round(1) }}%"></div>
            </div>
        </div>
        {% endif %}

        <!-- Features -->
        {% for feature in results.features %}
        <div class="feature">
            <div class="feature-header">
                <div>
                    <span class="feature-title">{{ feature.feature_name }}</span>
                    <span class="feature-id">({{ feature.feature_id }})</span>
                </div>
                <div class="feature-stats">
                    {% set feature_passed = feature.scenarios | map(attribute='summary.passed') | sum %}
                    {% set feature_failed = feature.scenarios | map(attribute='summary.failed') | sum %}
                    {% set feature_skipped = feature.scenarios | map(attribute='summary.skipped') | sum %}
                    <span class="stat-passed">{{ feature_passed }} passed</span>
                    {% if feature_failed > 0 %}
                    <span class="stat-failed">{{ feature_failed }} failed</span>
                    {% endif %}
                    {% if feature_skipped > 0 %}
                    <span class="stat-skipped">{{ feature_skipped }} skipped</span>
                    {% endif %}
                </div>
            </div>
            <div class="feature-content">
                {% for scenario in feature.scenarios %}
                <div class="scenario">
                    <div class="scenario-header">
                        <div>
                            <span class="scenario-name">{{ scenario.scenario_name }}</span>
                            <span class="scenario-id">{{ scenario.scenario_id }}</span>
                        </div>
                        {% if scenario.summary.failed > 0 %}
                        <span class="badge badge-failed">{{ scenario.summary.failed }} failed</span>
                        {% elif scenario.summary.skipped > 0 %}
                        <span class="badge badge-skipped">{{ scenario.summary.skipped }} skipped</span>
                        {% else %}
                        <span class="badge badge-passed">{{ scenario.summary.passed }} passed</span>
                        {% endif %}
                    </div>
                    <div class="executions">
                        {% for execution in scenario.executions %}
                        <div class="execution">
                            <div class="execution-header">
                                <span class="execution-name">{{ execution.test_name }}</span>
                                <span class="execution-duration">{{ "%.3f"|format(execution.duration) }}s</span>
                            </div>
                            {% if execution.status == 'skipped' and execution.skip_reason %}
                            <div class="skip-reason">{{ execution.skip_reason }}</div>
                            {% endif %}
                            {% if execution.tags and execution.tags | length > 0 %}
                            <div class="tags">
                                {% for tag in execution.tags %}
                                <span class="tag">{{ tag }}</span>
                                {% endfor %}
                            </div>
                            {% endif %}
                            {% if execution.parameters and execution.parameters | length > 0 %}
                            <div class="params">
                                {% for key, value in execution.parameters.items() %}
                                {{ key }}={{ value | tojson }}{% if not loop.last %}, {% endif %}
                                {% endfor %}
                            </div>
                            {% endif %}
                            {% if execution.steps %}
                            <div class="steps">
                                {% for step in execution.steps %}
                                <div class="step">
                                    {% if step.status == 'passed' %}
                                    <svg class="step-icon passed" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                    </svg>
                                    {% else %}
                                    <svg class="step-icon failed" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                                    </svg>
                                    {% endif %}
                                    <span class="step-description">{{ step.description }}</span>
                                    <span class="step-duration">{{ "%.3f"|format(step.duration) }}s</span>
                                </div>
                                {% if step.error %}
                                <div class="error-message">{{ step.error }}</div>
                                {% endif %}
                                {% endfor %}
                            </div>
                            {% endif %}
                            {% if execution.error %}
                            <div class="error-message">{{ execution.error }}</div>
                            {% endif %}
                        </div>
                        {% endfor %}
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
        {% endfor %}

        <footer>
            <p>
            <strong>Generated by SpecLeft.</strong>
            </p>
            <p>
            Enforcement is optional.
            Learn how to enforce system behaviour in CI:
            <a href="https://specleft.dev/enforce">specleft.dev/enforce</a>
            </p>        
        </footer>
    </div>
</body>
</html>
